# specify_df() errors if arguments are not hrmn_spec objects

    Code
      specify_df(col2 = 123)
    Condition
      Error in `specify_df()`:
      ! All arguments must be `hrmn_spec` objects.
      x Argument `col2` is not a `hrmn_spec` object.

